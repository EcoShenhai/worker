# Branding assets

Place the official crest/logo here as `logo.png` (PNG, ideally ~600px wide,
transparent background). The DOCX renderer picks it up automatically for the
government letterhead. If the file is absent, documents render with the text
header only.
